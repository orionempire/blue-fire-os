/**************************************************************************
 *	bluefire-os
 *	Version: 00.00.00
 *	Author: David Davidson
 *	Name: string.c
 *	Created: Jan 12, 2012
 *	Purpose:
 *  Usage:
***************************************************************************/
#include <common_include.h>


//strlen - calculate the length of a non-fixed length string
u32int strlen(const s08int *str) {
	const s08int *s;
	if (str == 0) return(0);
	for (s = str; *s; ++s);
	return(s-str);
}
